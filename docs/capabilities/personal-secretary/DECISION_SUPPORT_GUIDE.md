# Decision Support Guide

Decision Support compares option values against named numeric criterion weights. Negative weights represent costs or disadvantages. The result exposes each weighted score, the sorted comparison, the criteria, and the leading recommendation so the user can inspect the basis of the advice.

The leading option is advisory. No option is executed automatically.
